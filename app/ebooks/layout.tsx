export const metadata = {
  title: "E-books Gratuitos — Futebol Interativo",
  description: "Acesse os e-books gratuitos do Futebol Interativo.",
};

export default function EbooksLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}